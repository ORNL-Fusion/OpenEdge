
seed                12345
dimension           2
boundary            r r p
global              gridcut 0.0 comm/sort no
create_box          -20. 20 -20. 20.8 -0.05 0.05

create_grid         100 100 1
balance_grid        rcb  cell

############################################------species in the plasma (current and future)-------#####################################
species droplet.species mist Li Li+ Li2+ Li3+


mixture             LigamentSource  mist        frac 1.0 nrho 1
variable pmass  particle mass

#Load initial conditions
read_particles source.2 0

variable dt   equal 1e-05
variable time equal 2.5 #1.5
variable N equal floor(${time}/${dt})
variable N1 equal floor($N/100)

variable min_dt equal 0.05
variable nevery equal 1 #${min_dt}/${dt}
variable temp equal 773.15 # Kelvin
variable rd equal 50e-6 # m
variable md equal 2.796017461694916e-10


# compute evap and emit if necessary

# viscous/gravity
variable te grid "10"
variable ni grid "1.5746e20"
variable ti grid "10"
variable vp grid "0"
variable Br grid "0"
variable Bt grid "0"
variable Bz grid "0"
fix BC viscous 1 2.0 1 plasma te ti ni vp bfield Br Bt Bz gravity 0 0 -9.8 mass ${md} radius ${rd} temp ${temp}

# save data
dump 10 particle all ${N1} case.2 id type x y z vx vy vz v_pmass temp radius

timestep ${dt}
stats ${N1}
stats_style step cpu np

run $N
