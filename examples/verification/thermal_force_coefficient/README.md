# Ion thermal-force coefficient verification

This one-step test compares the OpenEdge velocity kick for B1+--B4+ and
C1+--C4+ against the mass-dependent DIVIMP `CIOPTN=1/3` coefficient. The
background ion is deuterium at 2.0 amu.

```sh
./run.sh /path/to/spa_mac_mpi
```
