# fix droplet/viscous — DEPRECATED

Predecessor of `grain/drag` reading plasma from per-grid variables/computes
instead of `fix background`. Same Epstein/Coulomb model, hardcoded Li
density. Use `grain/drag` in new decks.
